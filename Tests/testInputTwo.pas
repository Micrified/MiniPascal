PROGRAM arethusa(x,y);
VAR p, q, k : INTEGER;
VAR r, s : REAL;
VAR fibs : array [1 .. 40] of INTEGER;

PROCEDURE cube(i,j : INTEGER; a : ARRAY [0 .. 20] of integer);
BEGIN
	fibs[4] := i * j * 3.14;
	a[i - j] := a[i] * a[j + p];
	n := n - (j DIV i)
END;

BEGIN
	p := 2.2;
	r := 3.14;
	s := 9023.3;
	cube(fibs, q, fibs);
		IF ((p * p) > (r * s)) THEN s := cube(6.6,q,q,s) ELSE r := cube(p,q,r) ;
	x := 5 + 3.14;
	q := p DIV (10 MOD (5 + 5))
	END.
